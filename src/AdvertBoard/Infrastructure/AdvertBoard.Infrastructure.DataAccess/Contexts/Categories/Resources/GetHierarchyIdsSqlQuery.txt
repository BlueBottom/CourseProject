WITH RECURSIVE r AS (
    SELECT c."Id"
    FROM public."Category" c
    WHERE "Id" IN ({0})
    UNION
        SELECT c."Id"
        FROM public."Category" c
        JOIN r ON c."ParentId" = r."Id"
)
SELECT * FROM r